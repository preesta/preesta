{{~ for section in sections ~}}
{{~ if !for.first ~}}
———
{{~ end ~}}
<b>{{ section.subject | html.escape }}</b>
{{~ if section.followup ~}}
<i>{{ section.followup | html.escape }}</i>
{{~ end ~}}

{{~ for build in section.builds ~}}
• {{~ if build.url ~}}<a href="{{ build.url }}">{{ build.name | html.escape }}</a>{{~ else ~}}{{ build.name | html.escape }}{{~ end }} — {{ build.release_date }}{{ if build.expired }} (expired){{ end }}
{{~ end ~}}

{{~ end ~}}
